import { WEBSITEDATA } from '../shared/websiteData';

export const initialState = {
    websiteData: WEBSITEDATA
};

export const WebsiteDataReducer = (state = initialState, action) => {
    return state;
}